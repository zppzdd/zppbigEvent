// 这个文件中用来保存每个页面中使用的数据信息，例如接口地址
// - baseUrl中保存的是基地址
var baseUrl = 'http://localhost:8000';


// - 后面的变量中保存的是每个接口的地址
var USER_LOGIN = baseUrl + '/admin/login';
var USER_LOGOUT = baseUrl + '/admin/logout';
var USER_GETINFO = baseUrl + '/admin/getuser';

